// Tailwind has been temporarily removed from the project
// All styling is now handled by SCSS (styles/main.scss)
// To re-enable Tailwind in the future, uncomment the plugin below:
//
// const config = {
//   plugins: {
//     "@tailwindcss/postcss": {},
//   },
// };
//
// export default config;

// Empty config for now (PostCSS still needs to run for other plugins if any)
const config = {
  plugins: {},
};

export default config;
